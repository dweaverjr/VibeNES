# Build output
build/
bin/
out/

# Compiled objects
*.obj
*.o
*.pch
*.gch
*.pdb
*.ilk
*.exp

# Libraries (don't commit built libs — vcpkg handles dependencies)
*.lib
*.dll
*.a
*.so
*.dylib

# Executables
*.exe

# vcpkg (cloned locally, not committed)
vcpkg/

# CMake user-local presets (CMakePresets.json IS committed)
CMakeUserPresets.json

# VS Code caches
.vscode/ipch/
.cache/

# Visual Studio (if opened with full VS instead of VS Code)
.vs/

# IDE files we don't need in repo
imgui.ini

# ROM files (game ROMs not committed)
roms/*.nes
roms/*.chr
roms/*.prg

# Test ROMs ARE committed — don't ignore tests/test_roms/
# (put standard test ROMs like nestest, blargg, etc. there)

# Temp files
*~
*.tmp
*.swp
*.bak
*.log
nul

# Personal notes
ideas.txt
